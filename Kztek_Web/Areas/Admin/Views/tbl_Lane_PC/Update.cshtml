@model Kztek_Model.Models.tbl_Lane_PC_Custom

@await Html.PartialAsync("Partial_Action", new Kztek_Library.Models.PartialActionModel<Kztek_Model.Models.tbl_Lane_PC_Custom>()
{
    Model = Model,
    Controller = "tbl_Lane_PC",
    Action = "Update",
    Page = 1,
    Data_Select_1 = (Kztek_Library.Models.SelectListModel_Chosen)ViewBag.LstLane,
    Data_Select_2 = (Kztek_Library.Models.SelectListModel_Chosen)ViewBag.LstPC,

});
@section scripts{
    <script src="~/modules/Parking/tblLaneController.js"></script>
    <script>
        $(document).ready(function () {
            $("#Countinue").click(function () {
                $("#SaveAndCountinue").val('true');
            });

            $("#BackToList").click(function () {
                $("#SaveAndCountinue").val('false');
            });
            //$("#lane_ID").change(function () {
            //   var str = "";
            //    var s = $(this).val();
            //    str = s + ",";
            //    $("#lanes").val(str);
            //});
            $("#pc_ID").change(function () {
                var vlue = $('#pc_ID').val();
                $('#pcid').val(vlue);

            });
            $("#lane_ID").change(function () {
                var str = "";
                var vlue1 = $(this).val();
                str = vlue1 + ",";
                $('#laneid').val(vlue1);

            });
            @*tblLaneController.ShowCam('@Model.C1','@Model.C2','@Model.C3','@Model.C4','@Model.C5','@Model.C6');

            $("#LaneType").change(function () {

                tblLaneController.ShowCam('@Model.C1','@Model.C2','@Model.C3','@Model.C4','@Model.C5','@Model.C6');
            })

              $("#PCID").change(function () {
              tblLaneController.ShowCam('@Model.C1','@Model.C2','@Model.C3','@Model.C4','@Model.C5','@Model.C6');
              });*@
        });
    </script>
}
